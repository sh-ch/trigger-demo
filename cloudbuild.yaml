steps:
- name: 'gcr.io/cloud-builders/go'
  args: ['install', '.']
  env: ['PROJECT_ROOT=hello']
- name: 'gcr.io/cloud-builders/go'
  args: ['build', 'hello']
  env: ['PROJECT_ROOT=hello']
- name: 'bash'
  args: ['-c', 'for i in {1..10000}; do echo "this is a test"; done']
logsBucket: 'gs://sc-show-logs'
options:
  logging: GCS_ONLY
